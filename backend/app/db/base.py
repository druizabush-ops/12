"""Единая точка декларативного Base для всех моделей.
Нужна, чтобы Alembic и приложение работали с одной метаданной схемой.
"""

from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    """Единый базовый класс ORM.
    Схема управляется миграциями, поэтому Base используется только для описания моделей.
    """
