# AdminModule (UI Module Standard)

## Назначение
Закрытый UI-модуль административного раздела. Отображает интерфейс внутри окна модуля без платформенной логики.

## module.path
`admin`

## Уровень доступа по умолчанию
`admin`

## Ограничения модуля (запрещено)
- выполнять API-запросы напрямую из компонента;
- реализовывать RBAC / permissions внутри модуля;
- знать про routing, sidebar, module registry;
- импортировать `ModuleContainer` или `ModuleFallback`;
- выполнять навигацию.

## PROMPT создания
`🧠 MASTER PROMPT — BLOCK 18 / MODULE SDK / MODULE TEMPLATE (PLATFORM STANDARD)`

## Как безопасно изменить модуль в будущем
1. Меняйте только UI-разметку и локальное отображение состояния.
2. Сохраняйте файловую структуру:
   - `AdminModule.tsx`
   - `index.ts`
   - `README.md`
3. Не переносите в модуль access/routing-логику.
4. Для нового поведения сначала расширяйте платформенный контракт, затем UI.
