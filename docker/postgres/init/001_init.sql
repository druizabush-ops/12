-- Минимальный SQL-скрипт инициализации базы данных.
-- Он существует для проверки запуска Postgres без бизнес-таблиц.
-- Минимальность нужна, чтобы создать только базовую схему.

CREATE SCHEMA IF NOT EXISTS core;
